import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  max-width: 1200px;
  width: calc(100% - 20px);
  padding: 0 10px 0 10px;
  position: relative;
  margin: 0 auto;
`
